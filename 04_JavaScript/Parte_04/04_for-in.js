   /*
        Estrutura de repetição - LOOP
        - for in
    */

    const users = {name: "Wallace", age: 33, street: "Rua dos cavaleiros"}

    for (const key in users) {
        
        console.log(`${key} : ${users[key]}`)

    }

